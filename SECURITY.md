# 安全策略 / Security Policy

## 支持的版本

| 版本 | 是否接收安全修复 |
| --- | --- |
| 5.3.4（最新） | ✅ |
| 5.1.0 | ⚠️ 仅严重问题 |
| 5.0.x | ⚠️ 仅严重问题 |
| 4.8.x | ⚠️ 仅严重问题 |
| 4.7.x 及更早 | ❌ 请升级 |

MyScreenDraw 是**单机离线**桌面程序：不联网、不上传数据、不内置更新服务。
因此绝大多数 Web 类漏洞不适用；真实攻击面集中在**打开外部文件**与**本地数据**。

---

## 报告漏洞

**请不要在公开 Issue 中提交安全漏洞。**

请通过以下任一方式私密报告：

1. GitHub 私密漏洞报告：仓库 **Security → Report a vulnerability**（推荐）
2. 若仓库已启用 GitHub Security Advisories，请使用上方私密漏洞报告；尚未启用时，请先通过仓库维护者公开的 GitHub 个人资料联系渠道私下联系，切勿在公开 Issue 中披露细节。

报告时请尽量包含：

- 受影响版本（帮助 → 关于，或 `version.py`）
- 操作系统与 Python 版本
- 复现步骤，以及能触发问题的**最小样本文件**
- 实际影响（崩溃 / 数据损坏 / 任意文件读写 / 代码执行）
- 可选：修复建议

### 响应节奏

| 阶段 | 目标时间 |
| --- | --- |
| 确认收到 | 3 个工作日内 |
| 初步评估 | 10 个工作日内 |
| 修复并发布 | 视严重程度，一般 30–90 天 |

我们会在修复发布后于 Release Notes 中致谢报告者（除非你要求匿名）。

---

## 已知攻击面

程序会解析下列**外部可控输入**，这是安全关注的重点：

| 输入 | 位置 | 现有防护 |
| --- | --- | --- |
| `.msd` / `.json` 项目文件（打开或拖入） | `main.py` `open_project_from_path` | 文件大小上限、schema 校验、数量上限、数值范围校验（`persistence.py`） |
| 自动保存文件 | `data/autosave/` | 同上 |
| 配置文件 `data/config.json` | 启动时读取 | 类型与范围回退 |
| 点名名单 `.txt` / `.csv` | 导入名单 | 文件大小上限（64 MiB）、行数上限（5000） |
| 导入的图片 / PDF | 菜单导入（`import_media`） | 文件大小上限（64 MiB）、图片最长边缩图（2560 px）、PDF 页数上限（50）、项目内嵌数据上限（`persistence.py`） |
| 计算器表达式 | 计算器面板 | AST 白名单，禁止函数调用与属性访问（`calculator.py`） |

### 安全设计要点

- **计算器不使用 `eval()`**：`calculator.py` 用 `ast.parse` 后按白名单遍历节点，
  只允许数字与四则/幂/取模等运算，不允许名称、属性、调用、下标。
- **写文件为原子替换**：`persistence.py` 的 `atomic_write_json` 先写临时文件、
  `fsync` 后 `os.replace`，避免写一半损坏原文件。
- **项目文件严格校验**：坐标必须有限、颜色必须是 `#` 开头字符串、
  对象数量与数值范围有硬上限，拒绝畸形文件而不是带病加载。

---

## 使用建议

- **不要打开来源不明的 `.msd` / `.json` 项目文件。** 它们是结构化数据，
  程序不会从中执行代码，但畸形文件仍可能造成崩溃或异常占用。
- 便携版请放在**当前用户有写权限**的目录（桌面、U 盘、`D:\` 等）。
  若放入 `C:\Program Files`，`data/` 与 `exports/` 可能因权限失败。
- `data/events.jsonl` 与 `data/app.log` 为**本地**运行日志，不会上传；
  但可能包含文件名等信息，分享前请自行检查。
- 点名名单 `data/roster.json` 含学生姓名，属于个人信息，请勿随发行包一起分发。

---

## 不属于安全漏洞的情形

以下问题请走普通 Issue，而非安全报告：

- 界面错位、布局问题
- 手绘识别不准
- 性能卡顿
- 打开自己创建的文件时崩溃（属于稳定性 Bug）
- 未签名可执行文件触发 SmartScreen / 杀毒软件告警
  （见 `docs/code-signing.md`，属于分发信任问题）
