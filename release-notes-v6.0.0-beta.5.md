# MyScreenDraw v6.0.0-beta.5

这是 v6 系列的第五个预览版，修复 beta.4 实屏反馈：工具栏过大压住任务栏、LOGO 与工具栏宽度不一致、绘图模式下主界面点不动。

ZIP SHA-256：`80BBE5214AC9A58964DED36546216908E3CE006C63444B026C0FF7A97496C7A8`

## 主要更新

- 按钮改为 56×44（图标上、文字下），竖版整栏收进可用屏幕（不压任务栏）；放不下时自动压矮，白板控制区可折成两列；窄屏横版折成两行。
- LOGO 与工具栏同宽（竖版）/同高（横版）。
- 工具栏和 LOGO 始终压在全屏画布之上，绘图模式下可以正常点击。
- 「穿透模式 / 文件 / 关闭」补齐图标。
- F12 进后台经主线程队列，不再跨线程杀定时器；后台时不会把主面板重新拉出来。
- 消除 `requestActivate` 与 `UpdateLayeredWindowIndirect` 报错。
- 托盘「重启软件」会真正拉起程序并恢复未保存工作。
- 工具栏拖动保存配置已防抖。

## 安装

1. 下载 `MyScreenDraw-v6.0.0-beta.5-windows-x64.zip`。
2. 解压整个文件夹。
3. 运行 `MyScreenDraw.exe`。

这是测试版本，欢迎通过 GitHub Issues 反馈问题。
