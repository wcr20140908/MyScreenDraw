# 更新日志 / Changelog

> 本文件记录 MyScreenDraw 的完整版本历史（含内部技术细节）。
> 面向用户的说明见 [README.md](README.md) / [README.en.md](README.en.md)。

## v5.3.3（当前）— 只弹一个键盘，且不再和输入法打架

### 两个键盘先后出现（教室里不可用）

5.3.2 的 `show()` 等 0.6 秒看键盘出不出来，没出来就顺手把另一个后端也启动了。而**冷启动的 osk 实测约 1.0 秒**才画出来——那个 0.6 秒的门槛正好卡在真实启动耗时上：暖启动 0.41～0.52 秒，看着一切正常；冷启动超时，于是 TabTip 闪一下、消失，osk 接着冒出来。

- **换后端不再是 `show()` 的事**。`show()` 只发起一次请求就返回，不阻塞、不试第二个；换后端交给调用方的定时器（3 秒没出现才升级，6 秒才下「弹不出来」的结论）
- **`enforce_single()` 兜底**：只要观察到一个键盘出现，另一个立刻关掉。两个同时在屏上这件事从机制上不可能
- **无触摸的机器直接用 osk**。TabTip 在没有数字化仪的机器上永远不会出现（COM Toggle 每次报成功、窗口恒 DWM cloaked），先等它 3 秒是纯粹的死时间——教室里那 3 秒就是「坏了」。实测键盘出现时间从 3.98 秒降到 0.48 秒。有触摸的机器仍然首选 TabTip
- **用户关掉的键盘不再被硬拉回来**，也不再被误报成「弹不出来」。改用 150ms 轮询而不是定点采样：键盘出现的时刻不可预测，定点只查一次会漏掉「出现过」，于是把用户主动关闭误判成弹不出来，报出彻底误导的提示

### 和实体键盘/输入法打架

- **抢激活会取消组字**。`_refocus_input()` 原先无条件执行 `activateWindow()` + `processEvents()`，即使焦点本来就在输入控件上也照跑。`activateWindow()` 会发 `WM_ACTIVATE`，Windows 收到后**取消失焦窗口正在进行的输入法组字**。实测文字面板打开后头 3 秒内这套动作跑 6 次，正好落在用户敲第一个词的时间里——中日韩输入法一个字也提交不出来，这就是「哪边都打不出字」。现在焦点已正确就立即返回，一步都不做
- **组字期间的原始按键不再被当字符插入**。软键盘注入的按键不走输入法，与实体键盘的组字同时到达：实测「ni」组字中途来一个 `z`，公式格子里变成 `z你`。现在公式模式在组字期间只认 `inputMethodEvent` 的提交串
- **新开的文本框不再继承上一个框的组字状态**。旧状态留着会让新框第一下按键被当成「组字期间」而丢掉

纯文本模式下、组字中途注入按键仍会插入那个字符——这与原生 `QTextEdit` 行为完全一致（已逐事件比对验证），是 Windows 上所有应用的表现，改它会有搞坏正常打字的风险。

### 测试

离屏 491 项通过（原 469），连续三轮稳定。真机上逐项实跑验证：键盘 0.48 秒出现、全程只有一个、面板不被键盘遮挡、输入法提交能穿过一整轮按钮操作、公式里无杂字符、切工具后键盘收起且轮询停表。

`tests/test_keyboard_delivery.py` 新增两项真机用例（默认跳过）：按面板按钮不得抢激活、组字要能穿过一整轮按钮操作。这两条离屏测不了——那里文字面板拿不到焦点，测到的会是平台限制而不是被测代码。

## v5.3.2 — 屏幕键盘真的能弹出来了

5.3.1 的键盘按钮**在任何冷启动的机器上都不可能工作**。它有时能用，纯属侥幸：如果本次登录会话里已经有别的东西启动过 TabTip，它就正常；重启之后，同一份代码一次也不行。用户报告的正是这个现象——「昨天多次启动完全正常，今天重启后一次也不行，代码没人动过一个字」。

三个 Windows 细节，每一个单独都足以让按钮失效：

- **两个键盘程序都无法用 `CreateProcess` 启动**。`TabTip.exe` 和 `osk.exe` 的清单都要求提升，普通用户会话下 `subprocess.Popen` 直接抛 `WinError 740`（请求的操作需要提升），异常被 `except Exception: continue` 吞掉。改用 `ShellExecuteW`，它走壳层的 UAC 路径，实测能起来
- **`ITipInvocation` 只在 TabTip 已经在跑时才注册**，否则 `CoCreateInstance` 返回 `REGDB_E_CLASSNOTREG`（0x80040154）。所以 COM 这条路**无法自举**：进程必须先在。与上一条合起来就是死锁，而死锁被会话状态掩盖，于是表现为「重启前好、重启后坏」
- **Win10 1809 以后，可见的键盘不是那个看起来该是的窗口**。`IPTip_Main_Window` 还在，但它是个 `0,0,0,0` 的占位窗口；真正的键盘由 `TextInputHost.exe` 绘制，是一个 **DWM cloaked**（而非 hidden）的 `CoreWindow`。对占位窗口查 `IsWindowVisible` 永远返回「不可见」，这也让面板的「躲开键盘」逻辑从未真正触发过

### 无触摸屏的机器现在也能用

Windows 会在没有触摸数字化仪的台式机上压制触摸键盘：`Toggle` 每次都返回成功，窗口却一直 cloaked，键盘根本不出现。现在按机器能力选后端——有触摸先试 TabTip，没触摸直接用 `osk.exe`（老式屏幕键盘，任何桌面都能弹出，向焦点窗口发真实 `WM_KEYDOWN`/`WM_CHAR`）；先试的那个没出现就换另一个。

- 32 位打包版本也能找到 `osk.exe`：32 位进程看到的 `System32` 被重定向到 `SysWOW64`（那里没有它），补上 `Sysnative` 这条不被重定向的路径
- **不再谎报成功**。`show()` 的返回值只代表「已发起请求」，界面隔 700ms 与 2.5s 各复查一次屏幕上到底有没有键盘；真没有就说明原因（本机无触摸屏 / 键盘服务不可用），而不是笼统的「不可用」。新增 `text_keyboard_no_touch` 提示语，8 种语言齐备

### 测试

- `tests/test_touch_keyboard.py` 重写（28 项）：查语法树确认不再用 `subprocess`/`CreateProcess`、冷机必须先起进程再 toggle、已弹出的键盘不能被 toggle 关掉、TabTip 不出现时必须回落 osk、占位窗口的 0 面积与 cloaked 状态都不算可见
- `tests/test_text_workflow.py` 新增 `KeyboardRequestTests`（9 项）：报成功但没出现必须说出来，而不是沉默假装成功
- `tests/test_keyboard_delivery.py` 新增（默认跳过）：离屏平台没有真实 Win32 焦点，所以「外部键盘的字符能否抵达输入控件」只能在真平台验。设 `MYSCREENDRAW_REAL_KEYBOARD=1` 运行
- **离屏运行禁止启动键盘**。键盘是系统级窗口，无论进程多么 headless 都会落在用户真实屏幕上。`launch_allowed()` 在离屏平台直接拒绝启动（`MYSCREENDRAW_NO_KEYBOARD=1` 可显式关闭）。此前测试会真的往屏幕上弹键盘，也正因如此，真键盘的矩形会和面板定位测试注入的假矩形打架

全套 469 项通过，连续三轮稳定。

## v5.3.1 — 修复 5.3.0 的十个报告问题

5.3.0 带着 381 项通过的测试发布，却有十个问题一上手就撞得到。原因不在数量而在**测试方式**：那 381 项全是直接调方法，没有一项按过鼠标、敲过键、打开过面板。所以没有任何测试发现「根本不存在让按键抵达画布的通路」。本版新增 `tests/test_text_workflow.py`，规则是**只走真实入口**——鼠标事件经 `mousePressEvent`/`mouseMoveEvent`/`mouseReleaseEvent`，按键送给真正持有焦点的控件，面板按钮走它们的处理函数。够不到的功能，用户也够不到。

### 键盘无法输入（问题 1、6）

三层原因叠在一起，逐层都要修：

- **没有可接收按键的控件**。触摸键盘把 `WM_CHAR` 发给【有焦点的窗口】，而画布是 `WindowDoesNotAcceptFocus`（点它要能绘图，不能抢激活），面板里也没有任何可聚焦控件。新增 `_TextInputEdit`：纯文本模式它就是真编辑器（保留输入法，中日韩候选窗需要真正可编辑的字段），公式模式截获按键转成对当前格子的插入
- **心跳把焦点抢走**。置顶重排会重新激活主面板；心跳每 500ms 跑一次，所以面板打开不到半秒键盘就失效。心跳现在会在文字面板开着时把焦点还回去
- **选中面板把焦点抢走**。`begin_text_edit` 在打开输入面板之后调 `position_selection_panel`，后者 `force_topmost(select_panel)` 顺带夺走激活。现在文字面板会被压在选中面板之上，并恢复焦点——同时也是问题 9 的修复
- **面板按钮持有焦点**。`show()` 之后 Qt 把焦点给 tab 序里第一个可聚焦控件（标题栏的 × 按钮），键盘于是把字符送给了按钮；按一下退格焦点又落在按钮上。面板上除输入控件外一切设为 `NoFocus`，这条从结构上消失
- **退格/换行按钮改了画布却没同步控件**，下次打字时控件把旧内容整体回写，刚才那一下退格白做。现在按钮操作后回灌控件

### 面板位置（问题 2、3、5、9）

5.3.0 给这个面板发明了第三套定位逻辑（贴屏幕底 / 贴键盘上沿），一次踩四个坑。改为复用项目既有的 `_floating_anchor`——所有子菜单和缩略图都走它，本来就处理任务栏避让与贴边翻面。

- **不再压住任务栏**：位置收进 `availableGeometry()`
- **不再被键盘挡住**：键盘矩形与面板重叠时把面板翻到键盘上方，且保证不被推出屏幕
- **不再乱跳**：位置只在打开时锚定一次，切符号组、退格、插入结构只把面板收回屏内，不重锚
- **层级正确**：文字/公式面板压在选中面板（复制/删除那一条）之上

### 点击与拖拽（问题 7）

点一下不再冒出文本框。5.3.0 把「拖得太小」按最小尺寸给一个框，结果在画布上点一下就出框。现在位移小于 12px 视为点击，什么都不做；确实拖动了但拖得小，才给最小尺寸的框。

### 虚空文本框（问题 4）

空框会被任何绕过 `end_text_edit` 的路径留在画布上，带着编辑虚线框却没人能编辑。修法两条：

- 编辑态作废统一放进 `load_page`——`new_page` / `switch_page` / `apply_snapshot` / 打开项目全都经过它，逐个调用点去改必然漏掉一个
- 新增 `discard_empty_text_items()` 兜底，切工具时清扫

### 切工具不收键盘（问题 8）

原先收键盘藏在 `editing_text_id is not None` 里，编辑态已结束但键盘还开着时收不掉，键盘留在屏上挡住板书而画布已不接受输入。现在切到非 TEXT 工具就无条件收面板和键盘。

### 白板模式（问题 10）

模型层本可工作，失效来自上面那几条焦点与面板问题。现已与批注模式行为一致：建框、打字、公式、翻页、写回页面全部通过端到端测试。

### 验证

- 全量回归 **440 项通过**（5.3.0 为 381），连跑 3 次结果稳定
- 新增 `tests/test_text_workflow.py` **58 项**端到端用例，覆盖键盘输入、按钮、点击与拖拽、虚空框、面板定位与避让、层级、切工具、白板模式、公式构建、撤销
- 十个报告问题逐条按真实操作流程复验通过

## v5.3.0 — 文本框重做 + 结构化公式编辑器

### 停笔定形：明显不可能成形时立刻放弃

- **修复「光环转完却纹丝不动」**：写字时每一笔都是开放曲线，识别器在头几行就会否掉它，但原先仍照常转满 650ms 进度环，到点了笔迹毫无变化——那圈光环纯属干扰。现在在进度环出现【之前】先判一次可行性，判定不可能就立刻收手：**一圈都不画，之后无论按住多久都保持原样静止**
- 判据与识别器**共用同一套早期否决**（`_prepare` / `_is_open_stroke`）。两边各写一份的话阈值一改就会漂移，出现「光环转完不变形」或反过来「明明能识别却不给光环」
- 判据刻意**保守**：只在确定不可能时才否（点数太少、太小、既非直线又未闭合）。闭合笔迹一律照常等满时间，因为它还要过重采样、拐角检测和残差校验，那些便宜不了。测试双向固定了这个不变量——识别器认得出的绝不能被否掉，被否掉的识别器必须也返回 None

### 文本框

- **拖拽定框**：像画图形一样拖出矩形，框内多行输入。拖得太小（其实是点击）按最小尺寸给一个框，而不是没反应——触屏上拖精确矩形很难
- **多行文本**：按行渲染，最宽的一行决定宽度
- **改色 / 改粗细**：沿用当前笔色与笔宽
- **旋转 / 缩放**：复用既有的选中变换
- **向后兼容**：5.2.x 写的文本对象没有新字段，仍按内容自适应渲染，位置和大小不变；打开旧项目再保存也不会凭空多出字段

### 结构化公式编辑器

- **点哪个格子就在哪输入**。公式是一棵树而不是 LaTeX 字符串：按「分数」直接出现上下两个空格子，点中哪个就往哪写。**没有 tokenizer、没有 parser**——输入只能来自按钮，所以用户不可能造出渲染器不支持的东西，「语法不支持」这类失败模式整个消失了
- 也因此不需要在字符串里管光标：触屏上没法让老师戳方向键去 `\frac{}{}` 的大括号之间
- 支持 **分数、上标、下标、根号、求和、积分**（后两者带上下限），可任意嵌套
- **排版引擎不依赖 Qt**（`formula.py`），只通过一个度量接口拿字宽和升降部。这样几何能用假度量做**精确断言**，而不是「看起来差不多」——排版回归没法靠肉眼断言
- **根号用 `QPainterPath` 自己画**，不用字体的 √ 字形：字形高度固定，套在分数上明显不够长，而 Microsoft YaHei 没有 OpenType MATH 表，取不到可拉伸变体。自己画则任意高度都贴合，且**保持矢量**，SVG/EPS 导出不退化成位图
- 嵌套字号有下限，再深也不会缩到手指点不中
- 公式树**深拷贝**进撤销快照：与实时对象共享同一棵树的话，编辑公式会就地改掉历史快照

### 输入方式

- **字母数字交给系统触摸键盘（TabTip）**。它自带输入法、8 国语言、手写识别和 emoji，自己重做只会更差。走官方 `ITipInvocation` COM 接口，回退到启动 `TabTip.exe`——服务没起来时 COM 调用会静默失败
- **符号面板加在键盘上方**，按类型折叠：希腊字母、运算符、集合与逻辑、微积分、公式结构。同一时间只展开一组——全铺开在触屏上是一片小按钮，必然误触
- 展开的符号出现在分组条**上方**，手指从下往上点开、符号就在指尖附近，不会被手挡住
- 面板按键盘的**实际矩形**避让：叠在键盘上的话符号按钮会被盖住点不到
- 键盘不可用时（策略禁用等）给出提示并继续工作，符号面板照常能用
- 纯 Unicode 符号**不经过排版引擎**，`drawText` 直接画——这一档接近零成本，却覆盖了课堂上的大部分需求

### 其它

- 文字/公式面板纳入截图隐藏、样式下发、owner 重绑等既有浮窗生命周期，否则导出会把它拍进画面
- 切换工具时收束文字编辑并收起键盘，避免键盘留在屏上挡住板书而画布已不接受输入
- **本版不做 Markdown**：它与结构化公式是两套编辑模型（字符流+标记 vs 树），塞进同一个框里交互会很别扭
- **验证**：全量回归测试由 247 项增至 381 项通过。新增公式排版与命中 51 项、文本框与公式编辑 59 项、停笔可行性判定 15 项、触摸键盘降级 9 项

## v5.2.2 — 自动保存压缩 + 缩略图层级修复

- **自动保存改为 gzip**（`.json.gz`）。一份自动保存是整页快照，每条线段一个对象、各自带完整 pen 描述和 36 字符 UUID，重复度极高。实测你机器上那份 1.54 MB 的纯笔迹自动保存压到 **0.04 MB（-97.6%）**
- **含图片的页面压不动，这是预期**。同一台机器上那份 6.84 MB 的自动保存有 **99.8% 是一张内嵌图片的 base64**（6.80 MB，仅 145 条线段），PNG 本身已压缩，gzip 只拿到 -24.7%。要缩小这类文件只能改数据模型（图片去重/引用），不是调 gzip 等级能解决的——已写成测试固定下来，免得将来有人看到「压缩率很低」去误调参数
- **向后兼容**：5.2.2 之前写的未压缩 `.json` 自动保存仍可枚举、读取、恢复、清理
- **保留窗口改为 72 小时**，按文件名里的时间戳判断创建时刻，而不是 mtime——mtime 会被复制、备份、同步工具改写，按它清理可能误删
- **同时设数量上限 400 份**。光按时间不够：每 30 秒一份，72 小时就是 8640 份，即使压缩后也有数 GB，比原来「只留 5 份」更糟
- **最新一份永远保留**：隔一周回来打开也该有东西可恢复，而不是因为「全都超过 72 小时」被清空
- **内容没变就不写**：原先不动画布也每 30 秒落一份完整快照，目录里全是彼此相同的文件。同一秒内多次写入不再互相覆盖（文件名追加序号）
- **修复清理函数里的 `NameError`**：异常分支引用了未定义的变量，删除失败时会把整个清理带崩，而不是记下这一份跳过
- **修复缩略图与子菜单抢置顶**：缩略图是独立浮窗、不在 `all_subs()` 里，`show_only_sub()` 原先完全管不到它。两者都调 `raise_floating`，而 `HWND_TOPMOST` 决定不了同层兄弟的高低——压在下面的那个点不动。现在打开任意子菜单会立刻收起缩略图，反向亦然（只做单向的话，关掉再开缩略图时子菜单还在，争抢原样复现）
- **修复缩略图关闭后实时渲染没停**：原先 `hide()` 之后的可见性判断挡住了计时器停止，`_thumbnail_live_timer` 仍以 150ms 节拍把整本白板逐页渲染成 pixmap，白占主线程拖慢正在书写的笔迹。现在无条件停
- **验证**：自动保存专项 20 项 + 缩略图层级 7 项，全量回归测试由 221 项增至 248 项通过

## v5.2.1 — 笔迹更接近真笔

- **速度→宽度**：真笔快写只留一道浅痕，慢写笔尖压得实、墨也有时间渗开。现在笔速会影响落墨粗细：快写变细，正常速度维持设定笔宽。这是「真实笔迹」里唯一不依赖硬件的一半——鼠标有速度，注入的触控也有，而压感和倾角在本机根本传不到 Qt
- **按物理尺寸测速**：阈值用 mm/s 而不是像素/秒，并优先采用直尺校准值。同一手速在 75 寸大屏和笔记本屏上的像素位移差好几倍，按像素算会让两块屏上的笔迹粗细完全不同
- **只变细，不变粗**：宽度系数上限锁在 1.0。原本想让慢写显粗，但「慢＝粗」与停笔定形正面冲突——停笔那 650ms 用户被迫按住不动盯着进度环，手的生理抖动（实测 1~2px）会被读成「极慢」，笔尖当场涨成墨疙瘩，正糊在用户盯着的那一点上（12px 笔宽实测涨到 16px）。试过用位移阈值和锚点净位移区分「抖」和「慢写」，都不成：±2px 抖动的对角位移约 2.83px，而认真慢写一步才 3px，间隔太窄分不开；真正能分开的是方向一致性，那是另一个量级的复杂度，且阈值只能在真机上对着真人的手调。锁住上限等于用放弃一半效果，换掉整类不依赖阈值调对的 bug
- **宽度限幅**：相邻线段的宽度变化不超过 18%，否则笔迹会呈串珠状一节粗一节细
- **每指一份速度状态**：`_speed_mm_s` / `_speed_at` / `_speed_anchor` / `_last_seg_width` 都进 per-pointer 字段表，两根手指的速度不会互相污染；新的一笔不继承上一笔的末速，否则起笔粗细随机
- **荧光笔不受影响**：宽度恒定的设计保持不变
- **屏幕尺度不在落笔路径上取**：`current_screen_calibration()` 要查屏幕对象、算 key、查校准字典。把它放进 `_begin_stroke` 后，触控注入测试里「合成鼠标笔画」竞态的复现率从 1/8 升到 5/8——落笔多出的这点开销足以让 Windows 补发的鼠标消息更容易抢在触控帧之前。改为仅在校准变更和配置读回时刷新
- **修复：多指开关未持久化**（v5.2.0 漏项）。`smart_multitouch_enabled` 没进 `collect_settings()`，重启即复位成开启。真机上误触严重时它是唯一的退路，而会自己忘掉的退路不算退路。缺键时保持默认开启，不会因为读到旧版 `config.json` 而静默关掉多指
- **验证**：速度映射专项 19 项（宽度系数单调性与钳制、停笔抖动不出疙瘩、宽度限幅、逐笔与逐指隔离、开关与荧光笔），全量回归测试由 198 项增至 221 项通过

## v5.2.0 — 多指同时书写

- **两指同时书写**：触控大屏上两名学生可以各写一笔，互不干扰。每个接触点持有一份独立的笔画上下文（笔画 id、点列、压感、停笔状态共 11 个字段），处理某根手指时换入换出，共享的页面内容（笔迹、图形、文字、图片）保持单一副本
- **每指独立停笔定形**：停笔判定计时器改为一指一个。A 指停住定形成标准图形时，B 指正在写的笔画不受任何影响；定形只替换该指自己的那一笔。多根手指可同时显示笔尖进度环
- **撤销改为按笔完成时间入栈**：撤销撤掉「最后完成的一笔」，与哪根手指先落笔无关。原先的整页快照无法表达「只撤这一笔」——A 指落笔后 B 指落笔，此刻的快照里已含 A 的半截墨，拿它撤销会连带抹掉 A 未画完的部分。笔画因此改用增量条目（记录「删掉 id 为 X 的线段」），其余操作（改色、变换、擦除、换页、导入）继续使用整页快照，两种条目共存于同一撤销栈
- **撤销不再打断正在书写的手指**：增量撤销不走整页替换，B 指画到半途时撤销 A 的笔画，B 的墨与笔画上下文完整保留，可继续画同一笔
- **工具作用域**：两指共用当前工具（`draw_state` 全局），批注笔与荧光笔接管多指；选择、图形、文字、橡皮、激光笔在多接触点下没有明确语义，交回 Qt 由主接触点合成鼠标事件，行为与 v5.1 一致
- **单指路径零改动**：只有真的出现第二根手指时才进入多指分支。单指书写与鼠标走的仍是原来的字段和代码路径，行为逐字节不变
- **系统手势打断**：`TouchCancel`（系统手势抢走触控序列）时，已经画上屏幕的墨按笔收尾入栈，而不是静默抹掉——用户看得见它，一次撤销即可去掉
- **触控注入测试设施**：新增 `tests/touch_inject.py`，用 Win32 Touch Injection（`InitializeTouchInjection` / `InjectTouchInput`）在没有触控屏的机器上注入真实多点触控。注入的接触点走完整硬件路径（HID → WM_POINTER → Qt → QTouchEvent），因此验证的是生产输入路径而非 Qt 层伪造事件。两条硬性前提写进模块文档：`init_injection()` 必须在 `QApplication` 构造之前调用（Qt 只在构造时枚举一次输入设备），注入按屏幕像素定位而非焦点（目标窗口必须真的在那个像素上置顶）
- **过滤 Windows 补发的合成鼠标事件**：主接触点除 `WM_POINTER` 之外还会收到一套传统鼠标消息（Qt 中表现为 `pointingDevice().type() == TouchScreen` 的 `QMouseEvent`）。不过滤则第一根手指被画两次，凭空多出一笔。判据是时间加位置——合成笔的落点必然压在某个接触点上，用户此前用真鼠标画的笔画不会；两个证据缺一不可，判不出来时一律保留，宁可多一笔可撤销的墨也不吞掉已画好的内容
- **验证**：多指专项 36 项（并发笔画、按完成时间撤销、每指停笔、工具作用域、合成鼠标过滤、鼠标路径回归），全量回归测试由 162 项增至 198 项通过；真实 `QTouchEvent` 端到端注入测试在真实桌面会话下单独运行（`python tests\run_touch_injection.py`），离屏平台无法承载注入，故不纳入 `unittest discover`。该层依赖真实桌面 Z 序，被其他窗口抢占时会跳过而非失败

## v5.1.1 — Qt 标准对话框国际化修复

- **文件选择器本地化**：文件名称、文件类型、打开和取消等 Qt 标准控件随系统语言显示
- **颜色选择器本地化**：基本颜色、自定义颜色、拾取屏幕颜色、RGB/HSV 字段、确定和取消等控件随系统语言显示
- **8 国语言支持**：中文、英语、法语、西班牙语、德语、俄语、韩语和日语均加载对应 `qtbase` 翻译
- **验证**：Qt 标准对话框专项 3 项、全量回归测试 162 项通过

## v5.1.0 — 文件工作流与导入稳定性

- **统一文件面板**：打开项目、保存项目、另存为、图片/PDF 导入和 PNG/PDF/SVG/EPS 导出集中到单一“文件”入口，项目/导入/导出保持分组且不改变当前绘图工具
- **文件操作验证**：新增另存为取消/新路径、入口唯一性、横竖布局与子面板锚点回归
- **修复图片/PDF 导入卡死**：大图在解码器内预缩放并限制总像素；PDF 改为逐页增量渲染，单批只建立一次撤销快照、保存和界面刷新，异常时完整回滚
- **兼容云端文件**：媒体选择改用 Qt 非原生对话框，OneDrive Files On-Demand 占位图片可正常选择并在读取阶段报告错误
- **修复 EPS 图片输出**：RGB 图片改用 `colorimage`，并剥离 Qt 行对齐填充字节
- **Qt 标准对话框国际化**：文件选择器与自定义颜色选择器加载对应 `qtbase` 翻译，中文及其余 7 种支持语言的按钮、标签和颜色字段均随系统语言显示
- **验证**：新增 8 国 Qt 对话框翻译回归，文件/颜色对话框专项 3 项通过；全量回归测试增至 162 项

## v5.0.0 — 首个公开稳定版

- **版本号统一**：`version.py` / README / SECURITY / 审计文档统一为 `5.0.0`
- **国际化完成**：补全 i18n `theme` key（修复测试套件失败）；计算器错误/溢出文案、
  点名导入对话框改走 `tr()`，消除硬编码中文
- **异常收敛**：保存/打开/导出/窗口创建改为捕获具体异常类型
  （`OSError` / `ValueError` / `json.JSONDecodeError`），经 `map_io_exception` 映射为
  友好的本地化错误对话框（文件缺失 / 无权限 / 磁盘满 / 坏 JSON / 项目不合法）
- **新增 SVG 矢量导出**（`QSvgGenerator`）与 **EPS 矢量导出**（`eps_export.py`，
  原生 PostScript；EPS 无法内嵌 CJK 字体，含中文文本在缺少中文字体的查看器中可能显示不全）
- **新增图片 / PDF 导入**：图片经 `QImageReader` 解码（最长边 2560px 自动缩图）、
  PDF 经 `QPdfDocument` 逐页导入（≤50 页）；均以 PNG→base64 内嵌进 `.msd` 项目文件
  （自包含，可分享可备份）
- **点名导入补上限**：文件大小 64 MiB、行数 5000
- **发行包合规**：PyInstaller 打包纳入 `LICENSE` 与 `THIRD_PARTY_LICENSES.txt`；
  关闭 UPX 压缩（降低杀软误报率）
- **统一文件面板**：打开项目、保存项目、另存为、图片/PDF 导入和 PNG/PDF/SVG/EPS 导出集中到单一“文件”入口，项目/导入/导出保持分组且不改变当前绘图工具
- **文件操作验证**：新增另存为取消/新路径、入口唯一性、横竖布局与子面板锚点回归；全量测试增至 159 项
- **修复图片/PDF 导入卡死**：大图在解码器内预缩放并限制总像素；PDF 改为逐页增量渲染，单批只建立一次撤销快照、保存和界面刷新，异常时完整回滚
- **兼容云端文件**：媒体选择改用 Qt 非原生对话框，OneDrive Files On-Demand 占位图片可正常选择并在读取阶段报告错误
- **导入验证**：真实 10,000×5,000 图片缩至 2,560×1,280；真实 20 页 PDF 逐页导入完成，专项回归与全量测试共 151 项通过
- **全部源文件补齐 SPDX 头**（GPL-3.0-or-later）
- **新增英文版 README**（`README.en.md`）、`CHANGELOG.md`；全量测试增至 151 项

## v4.8.1 — UI 回归修复

- 撤销普通按钮的全局 `min-height`：Qt 样式中的最小高度会与上下 `padding` 相加，导致每颗按钮
  实际被撑到 50px 以上；竖版主面板由约 870px 恢复到约 394px（白板关闭）
- 批注设置面板恢复紧凑：色块、颜色预览条、粗细标签、滑块不再拉伸错位
- 横版白板控制区改为紧凑两行，不再将 5 个按钮横向摊开、挤乱主题/退出等后续按钮
- 增加 UI 尺寸回归测试，直接约束主面板高度、普通按钮高度、批注子菜单高度与内部顺序
- 同步修复审查发现的笔侧键/中键打断笔画、撤销后旧停笔计时让笔迹复活、抓屏后实时缩略图
  停更、原地移动/缩放/旋转/改色时缩略图不更新，以及重页面缩略图边界扫描过慢

## v4.8.0 — 触控大屏专项

- **修复启动即崩溃**：样式表引用的 `TOUCH_*` 常量从未定义，`apply_theme()` 一进构造函数就抛
  `NameError`。本版补齐这组常量，并把它们定为全局触控尺寸基准（按钮/滑块/色块 38px 级）
- **旋转键改为图标**：用 `QPainter` 程序化绘制圆弧箭头（不依赖字体里的 ⟳/emoji 字形，任何
  系统都不会显示成方框），去掉文字，不再挤占、遮挡标题栏；同时去掉它的提示气泡
- **提示气泡不再被画布压住**：气泡是 Qt 自建的 `QTipLabel` 顶层窗口，不在 owner 链里，置顶
  心跳半秒内就会把全屏画布推到它上面，只剩一角露出——现在心跳会把气泡一并抬到画布之上
- **白板缩略图实时渲染**：面板打开期间按内容指纹比对，落墨即现。当前页取画布实时内容（不再
  等松手写回 `pages`，否则永远慢一笔）；单页内容过多导致单帧渲染变慢时自动退避到慢节拍
- **横版 UI 整改**
  - 白板控制区按方向重排（竖版两行、横版一行），不再把「上页/下页/新页/黑板」的文字压掉；
    横版工具栏高度改由内容决定，不再写死 70px
  - 子菜单锚定到触发它的功能键：横版开在该键正下方，竖版与该键同高，不再一律贴到最左上角
  - 主面板贴屏幕下沿时子菜单翻到主面板上方、贴右沿时翻到左侧；拖动主面板时子菜单实时跟随
    并按需换边，不再钉死不动
  - 横竖切换后立即把面板钳回当前屏幕可用区，修复贴边旋转后跑出屏幕（竖转横、横转竖都修）
- **手绘识别重做分类环节**：由「阈值级联」改为「把三角形/矩形/菱形/平行四边形/梯形/圆/椭圆
  都建出来，逐个量重采样点到轮廓的平均距离，择优采用」；评分用未摆正的形状，避免略歪的矩形
  输给平行四边形；只检出 3 个拐角时会补一个「离三角形轮廓最远」的顶点再试四边形。
  用合成手绘样本（工整/潦草两档）扫参标定后：潦草梯形被判成椭圆的比例从 99% 降到 19%，
  潦草平行四边形被判成三角形/其它的比例从 100% 降到 13%
- **智能识别改为「停笔定形」**：画完把笔停在图形末端不抬起满 0.65 秒才转换，停留期间笔尖显示
  进度环；抬笔＝保留手绘。定形后不必抬笔即可接着画下一笔
- **触控优化**：命中容差与选中手柄按手指落点精度放宽，缩略图支持手指甩动滚动，并关闭 Windows
  对本程序窗口的「按住变右键 / 甩动手势 / 按住等待光环」——按住变右键会直接破坏停笔定形

## v4.7.5

- 修复图形「⋯」几何构造菜单被画布压在底层：该菜单是刻意不挂父窗口的 `QMenu`，没有 owner
  保护，而置顶心跳每 500ms 就 `force_topmost(画布)` 把全屏画布拉到置顶层最顶端——菜单弹出
  半秒后就被画布盖住，画布上的笔迹直接压在菜单文字上、点击也穿到画布。现在弹出期间暂停
  置顶心跳（与取色/校准/导入名单等模态对话框一致），并在菜单原生窗口创建后再矫正一次 Z 序
- `raise_floating()` 新增 `bind_owner=False`：对 `QMenu` 这类 Qt 自管的弹出窗口只矫正
  Z 序，不改写它的 `GWLP_HWNDPARENT`，避免干扰 Qt 的弹窗与失焦关闭逻辑
- 新增回归测试：断言菜单弹出期间心跳已停、关闭后恢复，异常路径下也能恢复

## v4.7.4

根治「子菜单不置顶、点不动」：

- 修复 `force_above()` 的参数写反：`SetWindowPos(hWnd, hWndInsertAfter)` 是把 hWnd 放到
  hWndInsertAfter 的**下面**，原实现 `SetWindowPos(面板, 画布)` 实际是把面板压到全屏画布
  之下。之前一直靠 owner（`GWLP_HWNDPARENT`）绑定被系统兜住才没暴露；owner 一旦失效，
  它反而主动把子菜单塞到画布底下，点击全部被画布吃掉
- 新增 `raise_floating()`：浮窗每次 `show()` 后重新绑定 owner 并排到画布之上。
  `hide()/show()` 不改 winId，而 `bind_topmost_stack` 只在 winId 组合变化时才重绑 owner，
  Qt 却可能在 show 时把 owner 重置掉——子菜单正好落在这个缝里。计算器/点名浮窗一直正常，
  是因为它们显示后会 `_bound_key=None` 触发重绑，现在这套动作补给了所有浮窗
- 子菜单、选中面板、缩略图面板、迷你计时器、计算器、点名面板统一走同一条置顶路径；
  截图/导出结束恢复这些浮窗时也重新绑定 owner
- 新增 `tests/test_window_stacking.py`：用真实离屏 Win32 窗口测量 Z 序，
  断言「owner 绑定缺失时浮窗仍在画布之上」

## v4.7.3

工具切换与窗口层级修复：

- 修复从橡皮切到其他功能后界面错乱：主栏「批注」原先只把高亮挪过去、并不真正切换工具，
  导致高亮显示在「批注」而画布仍停在橡皮——鼠标继续拖着橡皮光标圈、点画布是擦除，
  且此后再点「橡皮」只会开关设置面板，高亮永久停在错误位置
- 主栏高亮改为 `draw_state` 的唯一函数（`sync_tool_highlight`），任何入口都不可能再出现
  「高亮在 A、实际工具是 B」；放置教具不再谎报当前工具为「工具」
- 「批注」会回到上次使用的普通笔 / 荧光笔 / 激光笔
- 修复子菜单、选中面板、缩略图面板、迷你计时器点不动：这些浮窗显示后只做了
  `force_topmost`，而全屏画布同处置顶层且兄弟窗口高低未定，浮窗可能落到画布之下，
  点击穿到画布上（橡皮态下直接擦掉内容）。现在统一补上 `force_above(浮窗, 画布)`

数据与稳定性修复：

- 修复「建了多页 → 退出白板 → 再进白板」时第 2 页及以后的内容被静默丢弃
- 修复打开合法项目文件可能直接崩溃：`size`/`width` 写成 24.0 这类浮点数时
  `QFont`/`QPen` 抛 TypeError，而 PyQt6 中 `paintEvent` 的未捕获异常会终结进程
- 修复空点多边形、选中对象已失效等边界情况在重绘时抛异常终结进程
- 修复旧格式文件里的矩形使「⋯」几何构造菜单点击无反应
- 缩略图面板未打开时不再逐页渲染整本白板（白板页数多时的翻页卡顿）
- 量角器贴近基线时读数可能反向读成 180° 的边界错误
- 项目文件 `schema_version` 字段异常时给出正常提示而非类型错误
- 激光笔松手后不再滞留一份整页快照

## v4.7.0

- 新增演示聚光灯：暗化全屏、亮区跟随鼠标，滚轮调节亮区大小，右键退出
- 随机点名抽中结果新增大字全屏投影展示（自动消失，点击/按键可立即关闭）
- 白板逐页导出 PNG/PDF 时自动叠加页码
- 支持把 `.msd` / `.json` 项目文件拖到主面板直接打开
- 智能图形识别改为「画完停留一段时间后再转化为直线」，避免落笔瞬间误触发
- 修复退出橡皮模式时橡皮光标不立刻消失的残影问题
- 穿透模式与画布交互状态真正隔离：进入穿透时清空选择/未完成图形/延迟识别并收起选择面板，穿透态不再绘制选择框等交互 HUD

## v4.6.1

- 直尺新增独立紫色宽度手柄，并支持 `Ctrl + 滚轮` 调整尺身宽度
- 厘米标签和实时 mm/cm 读数改为屏幕坐标绘制，避免旋转、画布边缘或标签相邻时被裁切遮挡
- 直尺宽度、命中范围和手柄位置使用统一动态几何，宽度调整不影响真实毫米比例

## v4.6.0

- 直尺采用按显示器校准的真实毫米/厘米比例，并支持实时 mm/cm 悬停读数
- 量角器支持实时角度测量和 Shift 整度吸附

## v4.5.1

- 恢复白板模式的“上页 / 页码 / 下页”换页按钮
- 修复右侧缩略图列表未加入布局导致内容不显示的问题

## v4.5.0

- 修复长时间运行后窗口层级反复重写导致的按钮闪烁、子菜单抢焦点和重叠问题
- 根据系统语言自动选择应用语言，支持中文、英语、法语、西班牙语、德语、俄语、韩语和日语

## v4.4.0

- 直尺刻度从 0 开始并按逻辑单位随缩放调整显示密度
- 量角器刻度按真实角度绘制并随缩放调整标签密度
- 白板默认仅显示上页/页码/下页，点击页码后在右侧显示大尺寸真实缩略图
- 框选仅选择图形，点击框外会退出当前选择
- 修复计算器等号后的数字和运算符输入行为

## v4.3.0

- 移除重复的“当前工具设置”入口，批注工具再次点击即可进入对应设置
- 修复切换橡皮擦后光标残留问题
- 重做直尺、三角板和量角器的几何绘制与动态刻度
- 修正 45° 三角板为实际等腰直角三角形
- 移除圆规工具

## v4.2.0

- 自动保存改为原子写入，降低异常退出造成 JSON 损坏的风险
- 新增项目数据校验，并支持 `.msd` 项目文件保存/打开
- 计算器改用 AST 白名单解析，不再执行任意表达式代码
- 改进多显示器面板位置恢复
- 白板新增页面缩略图导航
- 新增核心纯逻辑单元测试和 PyInstaller 便携版构建脚本

## v4.1.0

- 主栏新增「工具」：计算器、随机点名、辅助作图、计时器、放大镜、导出
- 「批注」整合普通笔 / 荧光笔 / 激光笔；激光笔颜色可调、不落墨
- 辅助作图工具：直尺、45°/30° 三角板、量角器（拖动/滚轮缩放/旋转柄）
- 随机点名支持导入 txt/csv、去重抽取与重置

## v4.0.0

- 选中工具条（复制/删除/更多操作）
- 框选支持直接点选
- 新增菱形和角图形
- 直线端点吸附
- 计时器功能
- 主面板可拖动并记忆位置

## v3.9.2

- 修复点击闪烁与子菜单叠影
- 优化窗口合成性能

## v3.9.1

- 修复面板被全屏画布压住问题

## v3.9.0

- 智能识别常驻
- 移除手写文字识别功能