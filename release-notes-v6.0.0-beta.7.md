# MyScreenDraw v6.0.0-beta.7

这是 v6 的预览版本，不是正式稳定版。

## 生命周期与白板

- 修复竖向工具栏贴近屏幕底部时覆盖 LOGO 的布局问题。
- 后台隐藏或退出时关闭页面预览并停止刷新；恢复界面不自动重开预览。
- 退出确认框的关闭按钮、Esc、取消及保存失败均保持此前的显示状态。
- 托盘退出入口先关闭菜单，再打开确认框；重复取消不会绕过保存确认。
- 重启前保存失败时中止重启；恢复失败时保留恢复文件。
- 为压缩自动保存文件增加解压后大小限制。

## 更新策略

- 新安装默认开启自动检查，保留已有关闭设置；加载设置后每 24 小时检查，首次约在运行 24 小时后，不在启动时立即检查。也可手动检查或关闭。
- 自动检查仅提示新版本，不自动下载或安装。
- 下载与安装分别需要用户确认。
- 更新资产使用 Windows x64 ZIP 格式。

## 使用

下载 `MyScreenDraw-v6.0.0-beta.7-windows-x64.zip`，解压后运行 `MyScreenDraw.exe`。
更新前建议备份重要项目。预览版仍可能存在未发现的问题。

## 完整性与限制

发布资产的 SHA-256 校验值随同名 `.sha256` 文件提供。校验值用于检测文件变化，不是数字签名；本版本未提供代码签名。
自动安装依赖 Windows PowerShell，以及安装目录的写入权限。

问题反馈：https://github.com/wcr20140908/MyScreenDraw/issues
