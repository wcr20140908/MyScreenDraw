# 环境变量文件（包含敏感信息）
.env

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# 虚拟环境
venv/
.venv/
ENV/
env/

# 本地 AI / IDE 状态
.claude/
.cursor/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# 用户数据
data/events.jsonl*
data/config.json
# 测试/探针留下的配置备份。gitignore 不支持行尾注释，注释必须独占一行，
# 否则「data/config.json.*  # 说明」会被当成含空格和井号的字面模式，一条都匹配不到。
data/config.json.*
data/roster.json
data/autosave/
# 兜底：自动保存目前写进 data/autosave/（上一行已挡住），这条防的是将来写盘路径
# 改动后板书内容直接落在 data/ 下而无人察觉——那是用户的课堂内容，不能进仓库。
data/autosave_*.json

# 导出文件
exports/

# 日志
*.log

# macOS
.DS_Store

# Public screenshots must be reviewed for personal/environment data before commit
screenshots/
